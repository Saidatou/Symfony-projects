
	<ul>
		{% for post in posts %}
			<li>

				<a href="{{ path('app_posts_show', { slug: post.slug }) }}" class="text-decoration-none">
					{{ post.title }}
				</a>
			</li>

		{% endfor %}
	</ul>

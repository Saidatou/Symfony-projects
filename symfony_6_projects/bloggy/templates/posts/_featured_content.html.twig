<h2>🧁 What's new?</h2>

<p>
    Uh oh! {{ totalPosts }} posts written so far.
</p>

<div>
    <h3>✨ Latest posts</h3>
    {{ include('posts/_list.html.twig', { posts: latestPosts }) }}
</div>

<div>
    <h3>💬 Most commented Posts</h3>
    {{ include('posts/_list.html.twig', { posts: mostCommentedPosts }) }}
</div>




{# <h2>🎁 What's new?</h2> #}
{# {% cache "total_posts_count" %} #}
{# <p>
	Uh oh!
	{{totalPosts}}
	posts written so far.
</p> #}
{# {% endcache %} #}

{# {% with {latest_posts: latest_posts(5)}%} #}
{# <div>
	<h3>✨ Latest Posts</h3> #}
	
	{# {% cache "latest_posts" %} #}
	{# {% cache "latest_posts;"~latest_posts|map(p=> p.id)|join(',')|shasum256 %} #}
	{# {{include('posts/_list.html.twig', {posts:latestPosts(5)})}} #}
{# {% endcache %} #}
{# </div> #}
{# {% endwith %} #}
{# {% with {most_commented_posts: most_commented_posts(5)}%} #}
{# <div>
	<h3>🎉 Most Commented Posts </h3> #}

	{# {% cache "most_commented_posts" %} #}
	{# {% cache "most_commented_posts;" ~most_commented_posts|map(p=> p.id)|join(',')|shasum256 %} #}
    {# {{include('posts/_list.html.twig', {posts:mostCommentedPosts(5)})}}
	 #}
{# {% endcache %} #}
{# </div>
{% endwith %} #}

{% extends "base.html.twig" %}

{% block title %}
	MCSPORTIFS COACHS !
{% endblock title %}


{% block body %}

	{{ include("partial/title_section.html.twig", {'name':'Coach'}) }}

	<div class="section">
		<div class="container">
			<div class="row">
				<div class="col-lg-6 col-md-6 mb-4 mb-md-0">
					<div
						class="product-image">{# <div class="product_img_box"><img src='/assets/uploads/image{{annonce.image}}' data-zoom-image="/assets/uploads/image/{{annonce.image}}" alt="{{ annonce.nickname }}"/> #}

					</div>

				</div>
			</div>
			<div class="col-lg-6 col-md-6">
				<div class="pr_detail">
					<div class="product_description">
						<h4
							class="product_title">{# <a href="#">{{ annonce.nickname }}</a> #}
						</h4>
						<div
							class="product_price">{# <span class="price">${{ annonce.title}}</span> #}


						</div>

						<div
							class="pr_desc">{# <p>{{annonce.description | raw}}</p> #}
						</div>

					</div>
					<hr/>


				</div>
			</div>
		</div>


	{% endblock body %}

{% extends 'base.html.twig' %}

{% block title %}Edit Address
{% endblock %}

{% block body %}


	{{ include("partial/title_section.html.twig", {'name':"Modifier l'adresse"}) }}
	<div class="login_register_wrap section">
		<div class="container">
			<div class="row justify-content-center">
				<div class="col-xl-6 col-md-10">
					<div class="login_wrap">
						<div class="padding_eight_all bg-white">
							<div class="heading_s1">
								<h3>Modifier l'adresse</h3>
							</div>
							<div class="content">
								{{ include('address/_form.html.twig', {'button_label': 'Enregistrer'}) }}

								<a class="btn btn-border-fill btn-block mt-2" href="{{ path('address_client') }}">retour liste des Adresses</a>

								{# {{ include('address/_delete_form.html.twig') }} #}


							</div>

						</div>
					</div>
				</div>
			</div>
		</div>
	</div>

	<h1>Edit Address</h1>


{% endblock %}
